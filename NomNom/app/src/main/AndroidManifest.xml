<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.ximena.nomnom">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".MainActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".HomeActivity" />
        <activity android:name=".RegisterActivity" />
        <activity android:name=".EditProfileActivity" />
        <activity android:name=".DiscoveryActivity" />
        <activity android:name=".ProductsActivity" />
        <activity android:name=".ProductActivity" />
        <activity android:name=".ConfigActivity" />
        <activity android:name=".FindRestaurantsActivity" />
        <activity android:name=".MapActivity" />
        <activity android:name=".AddRestaurantActivity" />
        <activity android:name=".RestaurantActivity" />
        <activity android:name=".HelpActivity" />
        <activity android:name=".SupportActivity" />
        <activity android:name=".FavoriteRestaurantsActivity"></activity>
    </application>

</manifest>